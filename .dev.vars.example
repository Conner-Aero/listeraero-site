# Copy this file to .dev.vars for local `npm run pages:dev` testing.
# In production, set these as environment variables/secrets in the
# Cloudflare Pages dashboard instead (Settings > Environment variables).

RESEND_API_KEY=re_your_resend_api_key
CONTACT_TO_EMAIL=conner@listeraero.com
CONTACT_FROM_EMAIL=Lister Aerospace Website <onboarding@resend.dev>
